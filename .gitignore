node_modules
dist
.turbo
.vscode
.DS_Store
*.log

# vttforge dev — saved Foundry data-dir path
.vttforge/

# vttforge build — release artifact
*.zip

# Local agent notes — never tracked
.claude/

# The SDK versions this module needs are not published yet, so a lockfile
# here can only point at a local checkout. Track it again once they ship.
pnpm-lock.yaml
